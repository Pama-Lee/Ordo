[package]
name = "ordo-proto"
version = "0.3.0"
edition = "2021"
description = "Protobuf definitions for Ordo rule engine"
license = "MIT"

[dependencies]
prost = "0.12"
tonic = "0.10"

[build-dependencies]
tonic-build = "0.10"
